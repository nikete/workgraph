//! GEPA (Generalized Evolutionary Prompt Architecture) — re-exported from the `gepa` crate.
//!
//! This module re-exports everything from the standalone `gepa` crate so that
//! workgraph code can continue using `crate::gepa::*` as before.

pub use ::gepa_rs::*;
